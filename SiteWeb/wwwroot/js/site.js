// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

details = document.getElementsByClassName("details");
noneModal = document.getElementsByClassName("none-modal")

console.log(details);
console.log(noneModal);

/*Array.from(details).forEach(el => {
    el.addEventListener("click", () => {
        noneModal.style.display = 'block';
    });
});
*/


function modal(Produit) {
    var Id = Produit
/*    document.getElementById('staticBackdropLabel').innerHTML = Produit.Nom
    toogleAllClasses(details, 'd-block')*/
};.